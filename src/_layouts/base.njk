<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">

    {% if title %}
      {% set pageTitle %}
        {{ title }} {{ site.titleSeparator}}
      {% endset %}
    {% endif %}
    <title>{{ pageTitle if title else '' }}{{ site.title }} {{ site.titleSeparator }} {{ site.author.name }}</title>

    <link rel="stylesheet" href="/static_assets/css/styles.css">
  </head>
  <body class="{{ bodyClass }}">
    {% include "navigation.njk" %}

    <main class="{{ bodyClass }}__main">
      {{ content | safe }}
    </main>
  </body>
</html>
